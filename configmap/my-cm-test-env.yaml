kind: ConfigMap
apiVersion: v1
metadata:
  name: my-cm
  namespace: default
data:
  # Connection database config
  database: mysql
  database_uri: mysql://localhost:3306

  #User inteface
  font.title: Arial
  background-colot: red
  theme.1: clean
  theme.2: dark
---
kind: Pod
apiVersion: v1
metadata:
  name: pod-cm-env
spec:
  containers:
  - name: my-container
    image: nginx
    envFrom:
    - configMapRef:
        name: my-cm